function firstLetterName(name) {
alert("The name " + name + " starts with the letter " + name[0]);
}

function divisibleByTwo(number) {
 if (number%2 === 0) {
     return true;
 } else {
     return false;
 }

}
function largestNum(arr) {
    var array = [1, 4, 7, 9, 17];
var largest= 0;

for (arr=0; arr<=largest; arr++){
    if (array[arr] > largest) {
        var largest = array[arr];
    }
}
    }




















firstLetterName("Jaden", "J");
divisibleByTwo(23);
largestNum(2, 5, 9, 22, 43)